# Dependencies for the real FAISS/BM25/cross-encoder RAG service
# (app.main:app + ui/streamlit_app.py). Excludes eval-only (ragas),
# fake-pipeline-only (qdrant-client, sqlalchemy, arq, redis, alembic,
# asyncpg), and dev/test-only packages from requirements.txt — none
# of those are imported by the code this image actually runs.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
rank-bm25==0.2.2
sentence-transformers==3.2.1
transformers==4.46.2
torch==2.5.1
numpy==2.1.3
faiss-cpu==1.9.0
streamlit==1.40.0
requests==2.32.3
structlog==24.4.0
httpx==0.27.2
openai==1.54.4
anthropic==0.39.0
